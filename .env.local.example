# URL of the backend API in /server (deployed on Render). See docs/RENDER_BACKEND_SETUP.md.
# Local dev default assumes `cd server && npm run dev` (see server/.env.example for its port).
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
